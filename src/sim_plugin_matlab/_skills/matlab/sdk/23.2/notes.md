# matlabengine 23.2 (MATLAB R2023b) notes

Older line. Pairs with MATLAB R2023b.

This file exists so the layer is non-empty and `verify_skills_layout`
passes. Engine 23.2 has slightly older numpy↔matlab.double semantics
than 24.x — overrides land here when they bite.
