# matlabengine 24.1 (MATLAB R2024a) notes

Most common stable combo. The patterns in `base/` work unchanged on
this engine version.

This file exists so the layer is non-empty and `verify_skills_layout`
passes.
