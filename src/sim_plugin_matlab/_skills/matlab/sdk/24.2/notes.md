# matlabengine 24.2 (MATLAB R2024b) notes

Current line. Pairs with MATLAB R2024b. The patterns in `base/` work
unchanged on this engine version.

This file exists so the layer is non-empty and `verify_skills_layout`
passes. Per-engine quirks land here as discovered.
